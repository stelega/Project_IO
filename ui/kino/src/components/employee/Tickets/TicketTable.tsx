import React from 'react';
import { TicketList } from '../../../models/Ticket';

interface TicketListProps {
  tickets: TicketList;
}
const TicketTable = (props: TicketListProps) => {
  return <div>tu ma być tabela :)</div>;
};

export default TicketTable;
